name: Sync docs/ from README

on:
  push:
    branches:
      - main
    paths:
      - README.md

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync docs/index.md and docs/categories/ from README.md
        run: node scripts/sync-docs-index.mjs

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.md docs/categories/
          if git diff --cached --quiet; then
            echo "docs/ already in sync."
          else
            git commit -m "chore: sync docs/ from README.md"
            git push
          fi
